# Library Management System - Layered Architecture

## 📋 Project Information
- **Student Name:** [นายจักรภัทร พรมทา]
- **Student ID:** [67543210013-8]
- **Course:** ENGSE207 Software Architecture

## 🏗️ Architecture Style
Layered Architecture (3-tier)

## 📂 Project Structure
```
layered-library/
├── public/                    # Frontend (Static files)
│   ├── index.html
│   ├── css/
│   │   └── style.css
│   └── js/
│       ├── app.js
│       └── api.js
│
├── src/
│   ├── presentation/          # Presentation Layer
│   │   ├── controllers/       # รับ request / ส่ง response
│   │   │   └── bookController.js
│   │   ├── routes/            # กำหนด API routes
│   │   │   └── bookRoutes.js
│   │   └── middlewares/       # Middleware เช่น error handler
│   │       └── errorHandler.js
│   │
│   ├── business/              # Business Logic Layer
│   │   ├── services/          # กฎและกระบวนการทางธุรกิจ
│   │   │   └── bookService.js
│   │   └── validators/        # ตรวจสอบข้อมูล
│   │       └── bookValidator.js
│   │
│   ├── data/                  # Data Access Layer
│   │   ├── database/          # การเชื่อมต่อฐานข้อมูล
│   │   │   └── connection.js
│   │   └── repositories/      # จัดการข้อมูลในฐานข้อมูล
│   │       └── bookRepository.js
│   │
│   └── shared/                # ส่วนที่ใช้ร่วมกันทั้งระบบ
│       └── errors/            # Custom Error Classes
│           ├── ValidationError.js
│           ├── NotFoundError.js
│           └── ConflictError.js
│
├── library.db                 # SQLite database
├── server.js                  # Entry point ของระบบ
├── package.json
└── package-lock.json
```
## 🎯 Refactoring Summary

### ปัญหาของ Monolithic (เดิม):
- โค้ดทั้งหมดอยู่รวมกันในไฟล์หรือโฟลเดอร์เดียว ทำให้งงและสับสนได้
- Business Logic ปะปนกับการจัดการ HTTP Request/Response
- การแก้ไขโค้ดส่วนหนึ่งอาจทำให้ส่งผลกระทบถึงส่วนอื่นด้วย
- ทดสอบและ debug ยาก

### วิธีแก้ไขด้วย Layered Architecture:
- แยกโค้ดออกเป็น 3 ชั้นหลัก ได้แก่ Presentation, Business และ Data
- Controller ทำหน้าที่รับแส่งข้อมูลเท่านั้น ไม่จัดการ logic ใดๆ
- Business Service รวมกฎและกระบวนการทางธุรกิจไว้ในที่ๆเดียว
- Repository แยกการเข้าถึงฐานข้อมูลออกจาก logic

### ประโยชน์ที่ได้รับ:
- โค้ดอ่านง่ายและเป็นระเบียบมากขึ้น
- แก้ไขหรือเพิ่มฟีเจอร์อื่นๆได้ง่ายโดยที่จะไม่กระทบส่วนอื่น
- ง่ายต่อการทดสอบ


## 🚀 How to Run

```bash
1. Clone repository
git clone [https://github.com/ch4kkapatPR/ENGSE207-midterm-individual--67543210013-8-.git]

2. Install dependencies
npm install

3. Run server
npm start

4. Test API
Open browser: http://localhost:3000
```

## 📝 API Endpoints
| Method | Endpoint              | Description             |
| ------ | --------------------- | ----------------------- |
| GET    | /api/books            | ดึงรายการหนังสือทั้งหมด |
| GET    | /api/books/:id        | ดึงข้อมูลหนังสือตาม ID  |
| POST   | /api/books            | เพิ่มหนังสือใหม่        |
| PUT    | /api/books/:id        | แก้ไขข้อมูลหนังสือ      |
| PATCH  | /api/books/:id/borrow | ยืมหนังสือ              |
| PATCH  | /api/books/:id/return | คืนหนังสือ              |
| DELETE | /api/books/:id        | ลบหนังสือ               |
